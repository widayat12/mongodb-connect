const mocha = require('mocha');
const assert = require('assert');

// describe test

describe('some demo tests', function() {

    // create tests
    it('add two numbers togather', function() {
        assert(2 + 4 === 5);
    })
})